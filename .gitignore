# Python bytecode / caches
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environment (recreate with `uv sync`)
.venv/
venv/

# Environment / secrets (e.g. PROXY_LIST)
.env
.env.*

# OS cruft
.DS_Store

# Assignment brief — kept locally for reference, not a deliverable
ASSIGNMENT.md
Netshort_Scraping_Assignment*.docx

# Note: netshort_series.csv IS committed — it is a required deliverable.
# uv.lock IS committed — it pins the dependency set.
